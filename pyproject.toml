[project]
name = "agent-worx"
version = "0.1.0"
requires-python = "==3.12.*"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff.lint]
banned-imports = ["langgraph.checkpoint.sqlite"]
